/**
 * Energy Guard repo defaults — used when `VITE_*` env vars are unset (e.g. fresh clone).
 * Override locally via `.env` (see `.env.example`).
 */
export const DEFAULT_SITE_THEME = "energyguard";
export const DEFAULT_LAYOUT_PRESET = "local";
