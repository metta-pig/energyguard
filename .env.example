# Optional — Energy Guard works without a `.env` file (defaults: energyguard + local).
# Copy only if you want to override theme or home layout for experiments:
#
#   cp .env.example .env
#
# VITE_SITE_THEME=energyguard
# VITE_LAYOUT_PRESET=local
